# SQLite database file — generated at runtime, not source
tasks.db

# Python bytecode cache — generated artifacts, not source
__pycache__/
*.pyc

# Virtual environment — local, machine-specific, rebuildable from requirements.txt
.venv/
